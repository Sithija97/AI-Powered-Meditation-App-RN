export enum UserRole {
  ADMIN = "admin",
  IDLE = "idle",
  MANAGER = "manager",
  COORDINATOR = "coordinator",
  DRIVER = "driver",
  HELPER = "helper",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}

export enum MaritalStatus {
  UNMARRIED = "unmarried",
  MARRIED = "married",
}
